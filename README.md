# VIBE v1.0 (AI-first planning + execution)

This repo contains a VIBE program: a set of `.vibe` files designed to be interpreted by AI agents.

The contract:
1) Generate planning documents (spec/*).
2) Execute the workflow using tool calls.
3) Prove success via validators.